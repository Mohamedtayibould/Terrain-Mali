server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Admin SPA
    location /admin {
        try_files $uri $uri/ /admin.html;
    }

    # Client SPA
    location / {
        try_files $uri $uri/ /index.html;
    }
}
